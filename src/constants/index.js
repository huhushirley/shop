import { createConstants } from 'utils';


module.exports = createConstants(
  // reset error message
  'RESET_ERROR_MESSAGE',
  // counter
  'INCREMENT',
  'DECREMENT',
  // add food number
  'ADD_FOOD_NUMBER',
  // reduce food number
  'REDUCE_FOOD_NUMBER',
);
